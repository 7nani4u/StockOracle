# ── 핵심 데이터 처리 ─────────────────────────────────────────
numpy>=1.26.0
pandas>=2.0.0

# ── 주가 데이터 수집 ─────────────────────────────────────────
yfinance>=0.2.36
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── 뉴스/RSS ──────────────────────────────────────────────────
feedparser>=6.0.10

# ── 유틸리티 ──────────────────────────────────────────────────
lxml>=4.9.0
holidays>=0.80

# ── market_briefing 모듈 의존성 ──────────────────────────────
# (requests, bs4, yfinance, lxml 은 위에서 이미 선언됨)
# Vercel Python 런타임: certifi 는 자동 포함되나 명시 권장
certifi>=2024.2.2

# ── ML 예측 파이프라인 (StockFlow 통합, Vercel 최적화) ────────
# LightGBM: 1차 모델 — Train 7x 빠름, 모델 37x 작음(34KB vs 1.3MB), 추론 5x 빠름, 과적합↓
# Vercel: manylinux py3-none wheel 1.4MB, 추가 네이티브 의존성 없음, cold start +0.05s
# scikit-learn: fallback/검증/보정용 — LightGBM 미가용시 HistGradientBoosting으로 자동 대체
# scipy: Platt calibration (없으면 미보정, 추론에는 불필요)
# narwhals: lightgbm 의존성 (자동 설치)
# 학습은 오프라인(scripts/train_ml_model.py), Vercel에는 추론(모델 pkl)만 배포
scikit-learn>=1.3.0
scipy>=1.10.0
lightgbm>=4.0.0
